---
interface Props {
  delay?: number;
  class?: string;
}

const { delay = 0, class: className = "" } = Astro.props;
---

<div class={`mf-reveal ${className}`} style={`--mf-reveal-delay: ${delay}ms`}>
  <slot />
</div>
